//
// Created by Juan Sebastian Quiceno Rico on 2019-08-22.
//

#include "../ObjectLiveness.h"
#include <iostream>

/*int main() {
    ObjectLivenessBuilder builder;
    ObjectLiveness attempt1 = builder.SetValue(2114).Build();

    // rvalue attempt
    ObjectLiveness attempt2 = ObjectLivenessBuilder().SetValue(1003).Build();

    // Validate if they are null
    if (&attempt1 == nullptr) {
        std::cout << "Attempt1 esta nulo";
    } else {
        std::cout << "Attempt1 no esta nulo y tiene valor: " << attempt1.GetValue() << std::endl;
    }

    if (&attempt2 == nullptr) {
        std::cout << "Attempt2 esta nulo";
    } else {
        std::cout << "Attemp2 no esta nulo y tiene valor: " << attempt2.GetValue() << std::endl;
    }
    return 0;
}*/